import 'package:flutter/material.dart';
import 'models/location.dart';

class LocatonList extends StatelessWidget {
  const LocatonList({Key? key}) : super(key: key);
  final List<LocatonList>locations;

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
